# Populated by Phase 1 of ROADMAP.md from source repos on 2026-07-24.
# Broader scope than portfolio.yaml: every project ever started, including
# parked and killed. Torus sweeps this for revive candidates. Möbius uses it
# for portfolio archaeology.
#
# Phase 1 scope: the six GitHub repos on the operator's list, cloned read-only
# and derived from their ProjectOS files and git state. Extraction was
# adversarially verified. Full review: reports/mobius/phase-1-registry-review.md.
# Tags: [HU] operator-authored content found in the source repo or an operator
# instruction, [AI] synthesized from repo evidence, [INFERRED] a guess with
# reasoning in a sibling comment, [GAP] nothing findable.
# Conventions (DECISIONS.md, 2026-07-24): repo holds the git remote URL;
# repo_local is an extension field for the operator's local checkout path;
# started stores the full ISO first-commit timestamp from git (%cI).

version: 1
last_updated: 2026-07-24

projects:
  # --- preflight -------------------------------------------------------
  # [AI] Full ProjectOS set present (PROJECT_OS.md, ROADMAP.md, PARKING_LOT.md,
  # AGENTS.md with CLAUDE.md symlink). Seven commits, one per build phase.
  - slug: preflight
    name: Preflight                    # [HU] PROJECT_OS.md title
    aliases: [Preflight, preflight, preflight-monorepo, "@preflight/core", "@preflight/cli", "@preflight/web"]  # [AI] package.json workspace names, plus "@preflight/cli" which appears only in a stale PROJECT_OS.md Stack line (kept for matching)
    status: active                     # [AI] ROADMAP.md board: Phase 7 Hardening is the next phase
    kind: commercial_bet               # [HU] revenue path: paywalled reports, $99 single audit, $249 bundle (ROADMAP.md Phase 6; PROJECT_OS.md free-tier decision)
    repo: https://github.com/snowfranco/Preflight   # [AI] git remote get-url origin
    repo_local: "[GAP]"                # [INFERRED] possibly ~/Projects/pre-flight (directory name match only, not walked)
    one_line: 'Production-readiness auditor for AI-generated ("vibe-coded") apps: given a repo, it produces a scored audit report of security and reliability findings tied to file-level evidence, and can generate fix PRs.'  # [HU] PROJECT_OS.md purpose, first sentence
    started: "2026-07-19T12:37:56Z"    # [AI] first commit
    keywords: [security audit, typescript, next.js, supabase, cli, claude agent sdk, stripe, vibe-coded apps, non-technical founders, production readiness]

  # --- watchtower ------------------------------------------------------
  # [AI] Row re-derived 2026-07-24: the initial Phase 1 walk found this repo
  # EMPTY (zero commits); the operator pushed its contents to GitHub later
  # the same day (18 commits, full ProjectOS set). Extraction adversarially
  # verified against a fresh clone.
  # kind: [INFERRED] the repo-only reading is personal (single-user, no
  # commercial layer in v1 by design), but it is tooling that feeds the
  # operator's Claude stack: PROJECT_OS.md frames it as the source layer for
  # Claude Code Routines, and mission-control's own overlays name Watchtower
  # as the HITL approval surface (agents/helix/OVERLAY.md,
  # agents/sphere/OVERLAY.md). Non-commercial either way
  # (DECISIONS.md, 2026-07-24).
  - slug: watchtower
    name: Watchtower                   # [HU] PROJECT_OS.md title
    aliases: [Watchtower, watchtower]
    status: active                     # [AI] ROADMAP.md board: B2v (real-provider validation) is the single next phase
    kind: infra                        # [INFERRED] see row comment; personal is the alternative reading
    repo: https://github.com/snowfranco/Watchtower  # [AI] git remote get-url origin
    repo_local: "[GAP]"                # [INFERRED] possibly ~/Projects/watchtower (directory name match only, not walked)
    one_line: "Watchtower is a self-hosted, single-user event hub that lets Claude react to the world instead of only answering when asked."  # [HU] PROJECT_OS.md purpose, first sentence
    started: "2026-07-18T20:36:43-04:00"  # [AI] first commit
    keywords: [mcp server, typescript, sqlite, telegram, webhooks, rss, event hub, claude code routines, email ingest, sms]

  # --- workoutapp ------------------------------------------------------
  # [AI] ProjectOS set present (PROJECT_OS.md, ROADMAP.md, PARKING_LOT.md,
  # CLAUDE.md). Docs were re-standardized to reality on 2026-07-15.
  - slug: workoutapp
    name: workoutapp                   # [GAP] app name undecided (PARKING_LOT.md Q1); folder name used. FitPulse is only an index.html placeholder.
    aliases: [workoutapp, FitPulse, workout-timer]
    status: active                     # [AI] ROADMAP.md: Phase 3 in progress
    kind: commercial_bet               # [HU] white-label B2B SaaS model documented in PROJECT_OS.md
    repo: https://github.com/snowfranco/workoutapp  # [AI] git remote get-url origin
    repo_local: "[GAP]"                # no matching directory name in ~/Projects
    one_line: "A premium, gender-neutral wellness workout timer app with a B2B white-label endgame."  # [HU] PROJECT_OS.md purpose, first sentence
    started: "2026-06-10T22:20:20Z"    # [AI] first commit
    keywords: [fitness, workout timer, white-label, b2b saas, wellness, react, typescript, coaches, localstorage, vite]

  # --- sienna ----------------------------------------------------------
  # [AI] ProjectOS set present (PROJECT_OS.md, ROADMAP.md, PARKING_LOT.md,
  # CLAUDE.md). Two commits; v1 shipped, quality fine-tuning is next.
  # kind: the repo reads as a content pipeline (the "writing" definition),
  # but the operator's Phase 1 note states Sienna is infra, a dependency of
  # the Cardioid agent, and the repo does not contradict that
  # (DECISIONS.md, 2026-07-24).
  - slug: sienna
    name: Sienna                       # [HU] PROJECT_OS.md title
    aliases: [Sienna, sienna, "AEO Newsletter Writing Partner"]
    status: active                     # [AI] ROADMAP.md: in-progress and next items present
    kind: infra                        # [HU] operator Phase 1 note; see row comment and DECISIONS.md, 2026-07-24
    repo: https://github.com/snowfranco/Sienna      # [AI] git remote get-url origin
    repo_local: "/Users/snow/Projects/sienna"       # [INFERRED] directory name match in ~/Projects, not walked
    one_line: "Turn Snow's real build and work notes into published Substack and LinkedIn content with minimal friction."  # [HU] PROJECT_OS.md purpose, first sentence
    started: "2026-07-14T21:49:28-04:00"  # [AI] first commit
    keywords: [aeo, newsletter, content-pipeline, substack, linkedin, nextjs, typescript, sqlite, llm, drafting]

  # --- gabay -----------------------------------------------------------
  # [AI] ProjectOS set present (PROJECT_OS.md, ROADMAP.md, PARKING_LOT.md,
  # CLAUDE.md). Repo docs also call the repo "pinai" (README quickstart,
  # CLAUDE.md); GitHub name is Gabay. Naming drift noted in the review report.
  - slug: gabay
    name: Gabay Legal para sa OFW      # [HU] repo docs title
    aliases: [Gabay, gabay, pinai, ofw, "OFW Legal AI"]
    status: active                     # [AI] ROADMAP.md: populated in-progress section, last commit 2026-07-14
    kind: commercial_bet               # [HU] revenue streams documented in ofw_legal_ai_foundational_docs.md
    repo: https://github.com/snowfranco/Gabay       # [AI] git remote get-url origin
    repo_local: "[GAP]"                # [INFERRED] possibly ~/Projects/pinai (repo docs call the repo pinai; name match only, not walked)
    one_line: "Legal-information product for Overseas Filipino Workers: verify a recruitment agency's license, check an employment contract against the standard, and answer OFW labor and legal questions."  # [AI] PROJECT_OS.md one-line purpose, first sentence, tightly trimmed; that doc self-declares as backfilled from code, so not operator-authored
    started: "2026-07-05T20:36:53-04:00"  # [AI] first commit
    keywords: [legal-ai, ofw, philippines, rag, tagalog, fastapi, pgvector, safety-stack, freemium, consumer-legal]

  # --- theframeshift ---------------------------------------------------
  # [AI] Partial ProjectOS set: DECISIONS.md, ROADMAP.md, PARKING_LOT.md
  # present; no PROJECT_OS.md, README.md, or AGENTS.md/CLAUDE.md, so one_line
  # comes from ROADMAP.md instead of a purpose section.
  # status: [INFERRED] the strict "shipped" rule (all phases done plus live
  # deployment) has no clean match: all board rows are shipped except 2C
  # (planned, deliberately held until real production failure modes are
  # observed) and 2D (cancelled), and a live deployment exists: the
  # com.frameshift.bot.plist LaunchAgent runs watcher/bot.py with RunAtLoad
  # and KeepAlive (DECISIONS.md, 2026-07-24).
  - slug: theframeshift
    name: The Frameshift               # [AI] brand name across repo docs
    aliases: [TheFrameshift, "Frameshift v2", frameshift_v2, "Frameshift bot", "@ai.xr.frameshift"]
    status: shipped                    # [INFERRED] see row comment
    kind: writing                      # [INFERRED] content automation for the Frameshift Instagram publication
    repo: https://github.com/snowfranco/TheFrameshift  # [AI] git remote get-url origin
    repo_local: "/Users/snow/Projects/frameshift_v2"   # [AI] com.frameshift.bot.plist runs watcher/bot.py from this path
    one_line: "A lean, two-mode carousel generation system running on a MacBook: send a URL to Telegram and get a branded Instagram carousel back in under 10 minutes, or let it run on a schedule with human approval for 3 to 5 carousels daily."  # [AI] ROADMAP.md, no PROJECT_OS.md exists
    started: "2026-07-06T10:05:37-04:00"  # [AI] first commit
    keywords: [instagram carousels, telegram bot, python, pillow, groq, news ingestion, content automation, ai news, spatial computing, macos launchagent]

  # --- mission-control -------------------------------------------------
  # [AI] Added 2026-07-24 at the operator's request ("this is the same work
  # we are doing now"). This IS the repo the registry lives in, so state was
  # derived from the canonical local working tree, not a clone. Phase 1 is
  # merged and pushed to the GitHub remote at HEAD 90f46dd. This repo is the
  # cockpit that holds the portfolio, not a bet: it is tracked here but
  # occupies no portfolio slot (DECISIONS.md, 2026-07-24; registry/portfolio.yaml).
  - slug: mission-control
    name: mission-control            # [HU] PROJECT_OS.md title
    aliases: [Mission Control, mission-control, missioncontrol, Mission-Control]  # [AI] docs title, folder name, GitHub repo name
    status: active                   # [INFERRED] under active development (commits 2026-07-24, most recent of any tracked project); Phases 0-1 shipped and pushed, 2-4 planned; the board has no next marker yet
    kind: infra                      # [AI] the portfolio operations substrate; supports every other project (PROJECT_OS.md purpose)
    repo: https://github.com/snowfranco/Mission-Control  # [HU] operator-provided remote; Phase 1 merged and pushed at HEAD 90f46dd
    repo_local: "/Users/snow/Projects/missioncontrol"    # [AI] this session's working directory (canonical for this project)
    one_line: "This repo is the Mission Control substrate: the portfolio registry (registry/), the agent overlays (agents/), the scheduler config (schedule/scheduler.yaml), and the inter-agent queues (queues/)."  # [HU] PROJECT_OS.md purpose, first sentence
    started: "2026-07-23T18:01:57-04:00"  # [AI] first commit
    keywords: [portfolio management, agent orchestration, openclaw, projectos, yaml registry, jsonl queues, scheduler, hitl, markdown]

  # --- superlearn --------------------------------------------------------
  # [AI] Added 2026-08-05 when manifold (the Superlearn editor agent) joined
  # the crew (agents/manifold/OVERLAY.md; DECISIONS.md 2026-08-05). The repo
  # was recently renamed from News-Papele; its GitHub description still
  # carries the legacy feed-reader framing. The app renders editions and a
  # theme map from Supabase; manifold writes them. Tracked here, occupies no
  # portfolio slot: the five slots are full, and displacement is an operator
  # decision (registry/portfolio.yaml). [GAP] operator: should superlearn
  # take a slot, and if so, what does it displace?
  - slug: superlearn
    name: Superlearn                   # [AI] repo name at github.com/snowfranco/Superlearn
    aliases: [Superlearn, superlearn, News-Papele, news-papele]
    status: active                     # [AI] app live against Supabase; manifold shipped 2026-08-05 (agents/manifold/)
    kind: personal                     # [INFERRED] learning tool for the operator; no commercial line anywhere in the repo
    repo: https://github.com/snowfranco/Superlearn
    repo_local: "[GAP]"                # no checkout under ~/Projects on this machine
    one_line: "Personal learning cockpit: a daily edition and theme map over the reader's own sources, rendered from Supabase and written by manifold."  # [AI] from the repo's src/types.ts contract and README
    started: "2026-04-28T01:55:05Z"    # [AI] GitHub repo created_at (as News-Papele)
    keywords: [learning, rss, editions, theme map, supabase, react, vite, zod, manifold, editor agent]

# --- EXAMPLE ROW (not real state) ---
# projects:
#   - slug: <project-slug>
#     name: <project name>
#     aliases: [<short>, <repo path>]
#     status: active | parked | killed | shipped
#     kind: commercial_bet | personal | infra | writing
#     repo: <local path or gh url>
#     one_line: <what it is>
#     started: YYYY-MM-DD
#     killed: YYYY-MM-DD             # only if status: killed
#     kill_reason: <one line>         # only if status: killed
#     keywords: [<for Torus matching>]
